{
  "name": "module.umdify",
  "version": "0.2.1",
  "main": "src/umdify.js",
  "dependencies": {
    "gulp-wrapper": "^1.0.0"
  },
  "devDependencies": {
    "del": "^2.2.0",
    "gulp": "^3.9.1",
    "gulp-eslint": "^2.0.0"
  },
  "scripts": {
    "setup": "npm install",
    "upgrade": "npm update",
    "lint": "gulp lint",
    "test": "gulp test",
    "build": "gulp build"
  }
}
